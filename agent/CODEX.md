# Codex Handoff

Implement RockFoundry as an MVP planning application. Read `AGENTS.md`, `PRD.md`, and `PROJECT_MANIFEST.json` first.

Build the vertical slice before billing: create a project from an idea, generate a contextual bounded interview round, save a versioned state, compute readiness and contradictions, and export consistent Markdown ZIP. Keep AI output schema-bound; deterministic renderers own the final package.

Do not add code generation, private-repo ingestion, teams, or auto-renewal. For each external integration, preserve server-side authorization and test the failure path.
